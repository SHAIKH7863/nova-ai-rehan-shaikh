import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.novaai.rehanshaikh",
  appName: "Nova AI",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
